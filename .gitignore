# Dependencies
/node_modules

# Environment variables
.env
*.env.*
!.env.example

# Build artifacts and logs
/build
/dist
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# OBS output files (raw videos)
/obs-output/

# Operating system-specific files
.DS_Store
Thumbs.db

# Old generated files (if you confirm they are no longer needed by the current setup)
# If watcher.js now ONLY generates into public/qrcodes and public/latest_booth_info.json,
# and booth_display.html + mobile_template.html handle things,
# then these old per-video directories/files can be ignored and eventually deleted.
# /qrcodes/
# /public/booth/
# /public/mobile/
